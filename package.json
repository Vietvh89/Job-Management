{
  "name": "jobflow-studio-viet",
  "version": "1.0.0",
  "private": true,
  "description": "JobFlow with Supabase authentication, shared workspace and revision conflict protection.",
  "scripts": {
    "start": "npm run build && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --check public/app.js && node --check public/cloud.js && node --check public/staff-data.js && node tests/audit.cjs && node tests/cloud.cjs && node tests/board.cjs && node tests/access.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  }
}
