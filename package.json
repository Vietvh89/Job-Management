{
  "name": "jobflow-studio-viet",
  "version": "1.0.0",
  "private": true,
  "description": "Portable static export of JobFlow; browser-local storage, no shared backend.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --check public/app.js && node --check public/staff-data.js && node tests/audit.cjs"
  },
  "engines": { "node": ">=22" }
}
